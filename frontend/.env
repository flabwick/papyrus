# Backend API Configuration - use the nginx URL!
PORT=4201
GENERATE_SOURCEMAP=false

# Backend API Configuration
# Production - points to nginx-proxied backend API
REACT_APP_API_URL=http://localhost:3001/api

# Environment Detection
# This helps the app know which environment it's running in
REACT_APP_ENVIRONMENT=development